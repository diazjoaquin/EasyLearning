const Comments = (/* {video} */) => {

return(
    <div>
        {/* {
            video.comments?.map(e => {
                return(
                    <div>
                        <p>{e.tittle}</p>
                        <p>{e.description}</p>
                    </div>
                )
            })
        } */}
        {/* form create comments */}
    </div>
)
}

export default Comments;