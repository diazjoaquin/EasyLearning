const Video = (/* {video} */) => {
    return(
        <div>
            {/* {
                video?.map(e => {
                    return(
                        <p>{e.urlVideo}</p>
                    )
                })
            }
            
            <Comments video={video} /> */}
        </div>
    )
}

export default Video;