const Video = (/* {video} */) => {
    return(
        <div>
            <h1>Renderizando todos los videos de este curso.</h1>
            {/* {
                video?.map(e => {
                    return(
                        <p>{e.urlVideo}</p>
                    )
                })
            }
            
            <Comments video={video} /> */}
        </div>
    )
}

export default Video;